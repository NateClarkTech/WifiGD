# Normalize line endings for all files that Git considers text files.
* text=auto eol=lf

# Asset Library ZIP: only include the addon (not the C++ source tree).
/**              export-ignore
/addons          !export-ignore
/addons/**       !export-ignore